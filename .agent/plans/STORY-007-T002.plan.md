# STORY-007-T002 — Order History Frontend Pages

**Status:** PENDING
**Story:** STORY-007 — Order history
**Task:** T002 — Build My Orders list page and order detail page
**Assigned to:** Agent
**Priority:** P1
**Sprint:** 2
**Complexity:** S

---

## Summary

Build the order history frontend: My Orders page with paginated order list (order number, date, status, total), order detail page (items, quantities, prices, address, status), empty state with catalogue link. Status communicated with text not colour alone. Protected routes.

---

## Files in Scope

### CREATE
- `frontend/dashboard/src/pages/OrdersPage.tsx` — order list page
- `frontend/dashboard/src/pages/OrderDetailPage.tsx` — single order detail
- `frontend/dashboard/src/components/OrderList.tsx` — table of orders with pagination
- `frontend/dashboard/src/components/OrderStatusBadge.tsx` — status with text label
- `frontend/dashboard/src/components/EmptyOrders.tsx` — empty state
- `frontend/dashboard/src/hooks/useOrders.ts` — fetch order list
- `frontend/dashboard/src/hooks/useOrder.ts` — fetch single order
- `frontend/dashboard/src/api/orders.ts` — add listOrders (modify existing file if created in STORY-006)
- `frontend/dashboard/tests/unit/OrdersPage.test.tsx`
- `frontend/dashboard/tests/unit/OrderDetailPage.test.tsx`
- `frontend/dashboard/tests/unit/OrderList.test.tsx`
- `frontend/dashboard/tests/unit/EmptyOrders.test.tsx`

### MODIFY
- `frontend/dashboard/src/App.tsx` — add /orders and /orders/:id routes (protected)
- `frontend/dashboard/src/components/Header.tsx` — add My Orders link (when authenticated)

### OUT OF SCOPE
- Order creation — STORY-006
- Admin order views — future story

---

## Acceptance Criteria Covered
| AC   | Covered by Task | Notes |
|------|----------------|-------|
| AC-1 | Yes | Orders listed with number, date, status, total, sorted desc |
| AC-2 | Yes | Order detail with items, quantities, prices, address, status |
| AC-4 | Yes | Pagination with 10 per page and navigation controls |
| AC-5 | Yes | Empty state with catalogue link |

---

## Implementation Steps
1. Write failing tests for OrderList (renders rows, pagination controls, column headers, axe-core)
2. Write failing tests for OrderStatusBadge (text label not colour alone)
3. Write failing tests for EmptyOrders (message + catalogue link)
4. Write failing tests for OrdersPage (loads data, shows list or empty state)
5. Write failing tests for OrderDetailPage (shows full order info)
6. Create TypeScript interfaces for order list
7. Add listOrders to orders API client
8. Implement useOrders and useOrder hooks
9. Implement OrderStatusBadge, EmptyOrders, OrderList
10. Implement OrdersPage and OrderDetailPage
11. Update App.tsx with protected routes
12. Update Header with My Orders link
13. Run lint → types → tests → Playwright → verify all green

---

## Dependencies
- Depends on: STORY-007-T001 (API), STORY-004-T002 (ProtectedRoute, Header)
- Blocks: (none)

---

## Role Annotations
(empty)
