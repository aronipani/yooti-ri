# STORY-001-T002 — Product Catalogue API Endpoints

**Status:** PENDING
**Story:** STORY-001 — Browse product catalogue
**Task:** T002 — Implement GET /api/v1/products and GET /api/v1/categories endpoints
**Assigned to:** Agent
**Priority:** P0
**Sprint:** 1
**Complexity:** S

---

## Summary

Build the API layer for browsing the product catalogue: list products with filtering by category, sorting by price, and cursor-based pagination (limit/offset). Also provide a categories list endpoint.

---

## Files in Scope

### CREATE
- `services/api_python/src/repositories/product_repository.py` — DB queries for products (list, filter, sort, paginate)
- `services/api_python/src/repositories/category_repository.py` — DB queries for categories
- `services/api_python/src/repositories/__init__.py`
- `services/api_python/src/services/product_service.py` — business logic (thin: delegates to repo)
- `services/api_python/src/services/category_service.py` — business logic
- `services/api_python/src/routes/products.py` — GET /api/v1/products route
- `services/api_python/src/routes/categories.py` — GET /api/v1/categories route
- `services/api_python/src/types/product.py` — Pydantic response schemas (ProductListItem, ProductListResponse, PaginationMeta)
- `services/api_python/src/types/category.py` — Pydantic response schemas
- `services/api_python/tests/unit/test_product_service.py`
- `services/api_python/tests/unit/test_category_service.py`
- `services/api_python/tests/unit/test_product_routes.py`

### MODIFY
- `services/api_python/main.py` — register products and categories routers

### OUT OF SCOPE
- Frontend — handled in T003
- Product detail endpoint — STORY-002
- Search endpoint — STORY-008

---

## Acceptance Criteria Covered
| AC   | Covered by Task | Notes |
|------|----------------|-------|
| AC-1 | Yes | GET /products returns name, price, thumbnail, stock_status |
| AC-2 | Yes | ?category= filter param, URL reflects filter |
| AC-3 | Yes | ?sort=price_asc param for sorting |
| AC-5 | Yes | ?page=&limit= params, default limit=20 |

---

## Implementation Steps
1. Write failing tests for ProductRepository (list, filter by category, sort by price, paginate)
2. Write failing tests for ProductService
3. Write failing tests for GET /products route (response shape, filter, sort, pagination)
4. Write failing tests for GET /categories route
5. Implement Pydantic schemas in `types/product.py` and `types/category.py`
6. Implement `product_repository.py` — async SQLAlchemy queries with filtering, sorting, offset pagination
7. Implement `category_repository.py`
8. Implement `product_service.py` — constructor DI for repository
9. Implement `category_service.py`
10. Implement `routes/products.py` — thin route, validate query params, call service, return response
11. Implement `routes/categories.py`
12. Register routers in `main.py`
13. Run lint → types → tests → verify all green

---

## Dependencies
- Depends on: STORY-001-T001 (models must exist)
- Blocks: STORY-001-T003 (frontend needs API)

---

## Role Annotations
(empty)
