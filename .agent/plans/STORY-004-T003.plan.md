# STORY-004-T003 — Login/Logout Frontend and Protected Routes

**Status:** COMPLETE
**Story:** STORY-004 — Sign in and out
**Task:** T003 — Login page, sign out in header, session persistence, ProtectedRoute wrapper
**Layer:** frontend
**Priority:** P0
**Sprint:** 1
**Complexity:** M (task 3 of 3)

---

## Files in Scope

### CREATE
- `frontend/dashboard/src/pages/LoginPage.tsx` — login form page
- `frontend/dashboard/src/components/LoginForm.tsx` — email + password form
- `frontend/dashboard/src/components/ProtectedRoute.tsx` — redirects unauthenticated to /login
- `frontend/dashboard/src/components/Header.tsx` — user name display, sign out button
- `frontend/dashboard/tests/unit/LoginPage.test.tsx`
- `frontend/dashboard/tests/unit/ProtectedRoute.test.tsx`

### MODIFY
- `frontend/dashboard/src/hooks/useAuth.ts` — add login, logout, checkSession
- `frontend/dashboard/src/api/auth.ts` — add login, logout, refresh calls
- `frontend/dashboard/src/contexts/AuthContext.tsx` — add login/logout state
- `frontend/dashboard/src/App.tsx` — add /login route, wrap protected routes

### OUT OF SCOPE
- Registration form (STORY-003-T003)
- Cart icon in header (STORY-005)

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-1 | Full | Login → redirect home, name in header, persists on refresh |
| AC-2 | Full | "Incorrect email or password" generic error |
| AC-3 | Full | Sign out clears session, redirects, blocks protected routes |
| AC-4 | Full | Expired session → redirect to login with expiry message |

---

## Implementation Steps
1. Write failing tests: LoginForm renders, validates, shows generic error, axe-core
2. Write failing tests: ProtectedRoute redirects unauthenticated, shows expiry message
3. Write failing tests: Header shows user name, sign out triggers logout
4. Add login/logout/refresh to auth API client
5. Update AuthContext with login, logout, checkSession on mount
6. Implement LoginForm, LoginPage, ProtectedRoute, Header
7. Update App.tsx routes
8. Run lint → types → tests → Playwright → green

---

## Dependencies
- Depends on: STORY-004-T002 (API), STORY-003-T003 (AuthContext)
- Blocks: STORY-007-T002 (protected routes), STORY-009-T002 (admin routes)
