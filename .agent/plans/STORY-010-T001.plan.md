# STORY-010-T001 — Auth Endpoint Rate Limiting

**Status:** PENDING
**Story:** STORY-010 — Rate limiting on authentication endpoints
**Task:** T001 — Apply Redis rate limiting to login, register, refresh with graceful degradation
**Layer:** api
**Priority:** P0
**Sprint:** 1
**Complexity:** S (task 1 of 1)

**Agent instruction:** Write the test demonstrating missing rate limiting FIRST. Confirm it fails. Then implement.

---

## Files in Scope

### CREATE
- `services/api_python/tests/unit/test_auth_rate_limiting.py`

### MODIFY
- `services/api_python/src/middleware/rate_limit.py` — ensure per-endpoint config, Retry-After header, graceful degradation on Redis failure
- `services/api_python/src/routes/auth.py` — apply rate limiter to login (10/15min), register (5/1hr), refresh (20/15min)

### OUT OF SCOPE
- Account lockout (STORY-004 — per-account, different mechanism)
- Frontend error display

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-1 | Full | Login: 10 req/15min → 429 with Retry-After |
| AC-2 | Full | Redis down → requests allowed, graceful degradation |
| AC-3 | Full | Register: 5 req/1hr → 429 with Retry-After |

---

## Implementation Steps
1. Write failing test: 11th login in 15min → 429 with Retry-After header
2. Write failing test: 6th register in 1hr → 429 with Retry-After header
3. Write failing test: 21st refresh in 15min → 429 with Retry-After header
4. Write failing test: Redis unavailable → request succeeds (graceful degradation)
5. Confirm all tests fail
6. Update rate_limit middleware with per-route config and Retry-After calculation
7. Add graceful degradation (try/except Redis, log warning, allow through)
8. Apply rate limiter dependencies to auth routes
9. Confirm all tests pass
10. Run lint → types → tests → green

---

## Dependencies
- Depends on: STORY-003-T002 (register route), STORY-004-T002 (login/refresh routes)
- Blocks: none
