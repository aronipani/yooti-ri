# STORY-008-T001 — Product Search API with Full-Text Index

**Status:** PENDING
**Story:** STORY-008 — Product search
**Task:** T001 — Implement GET /api/v1/products/search with PostgreSQL full-text search, query sanitisation, and max length validation
**Assigned to:** Agent
**Priority:** P1
**Sprint:** 2
**Complexity:** S

---

## Summary

Build the search API: GET /products/search?q=&page=&limit= with PostgreSQL full-text search (GIN index on name + description), parameterised queries (no SQL injection), max query length 200 chars, pagination. Migration adds the full-text search index.

---

## Files in Scope

### CREATE
- `services/api_python/alembic/versions/006_add_product_search_index.py` — GIN index on tsvector(name, description)
- `services/api_python/src/types/search.py` — Pydantic schemas for search response
- `services/api_python/tests/unit/test_search_route.py`
- `services/api_python/tests/unit/test_product_search_repository.py`

### MODIFY
- `services/api_python/src/repositories/product_repository.py` — add search method with full-text query
- `services/api_python/src/services/product_service.py` — add search_products method
- `services/api_python/src/routes/products.py` — add GET /products/search route

### OUT OF SCOPE
- Frontend — T002
- Elasticsearch/external search — use PostgreSQL built-in

---

## Acceptance Criteria Covered
| AC   | Covered by Task | Notes |
|------|----------------|-------|
| AC-1 | Partial | API returns matching products by name or description |
| AC-2 | Partial | API returns empty results with count=0 |
| AC-4 | Yes | SQL special characters treated as literal (parameterised) |

---

## Implementation Steps
1. Write failing test: search by product name returns matching products
2. Write failing test: search by description returns matching products
3. Write failing test: SQL special characters in query don't cause errors
4. Write failing test: query > 200 chars returns 422
5. Write failing test: empty query returns 422
6. Write failing test: search results are paginated
7. Create migration with GIN index on tsvector
8. Add search method to product_repository (ts_query with parameterised input)
9. Add search_products to product_service (validate length, delegate)
10. Add GET /products/search route
11. Run lint → types → tests → verify all green

---

## Dependencies
- Depends on: STORY-001-T001 (Product model), STORY-001-T002 (base product routes)
- Blocks: STORY-008-T002 (frontend)

---

## Role Annotations
(empty)
