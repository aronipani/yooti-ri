# STORY-001-T003 — Product Catalogue Frontend

**Status:** PENDING
**Story:** STORY-001 — Browse product catalogue
**Task:** T003 — Build the catalogue page with product grid, category filter, sort, and infinite scroll
**Assigned to:** Agent
**Priority:** P0
**Sprint:** 1
**Complexity:** M

---

## Summary

Build the React frontend for the product catalogue: a responsive product grid with category filter dropdown, price sort control, out-of-stock badges, and infinite scroll pagination. All components must pass axe-core accessibility checks.

---

## Files in Scope

### CREATE
- `frontend/dashboard/src/pages/CataloguePage.tsx` — page component with filter/sort state and infinite scroll
- `frontend/dashboard/src/components/ProductGrid.tsx` — grid layout of ProductCard components
- `frontend/dashboard/src/components/ProductCard.tsx` — individual product card (name, price, thumbnail, stock badge)
- `frontend/dashboard/src/components/CategoryFilter.tsx` — category dropdown filter
- `frontend/dashboard/src/components/SortControl.tsx` — sort by price control
- `frontend/dashboard/src/hooks/useProducts.ts` — data fetching hook with infinite scroll (page tracking)
- `frontend/dashboard/src/hooks/useCategories.ts` — fetch categories
- `frontend/dashboard/src/types/product.ts` — TypeScript interfaces for Product, Category, API responses
- `frontend/dashboard/src/api/products.ts` — API client functions (getProducts, getCategories)
- `frontend/dashboard/tests/unit/ProductCard.test.tsx`
- `frontend/dashboard/tests/unit/ProductGrid.test.tsx`
- `frontend/dashboard/tests/unit/CataloguePage.test.tsx`
- `frontend/dashboard/tests/unit/CategoryFilter.test.tsx`
- `frontend/dashboard/tests/unit/useProducts.test.tsx`

### MODIFY
- `frontend/dashboard/src/App.tsx` — add route for catalogue page (or homepage)

### OUT OF SCOPE
- Product detail page — STORY-002
- Cart functionality — STORY-005
- Search — STORY-008

---

## Acceptance Criteria Covered
| AC   | Covered by Task | Notes |
|------|----------------|-------|
| AC-1 | Yes | Product grid showing name, price, thumbnail, stock status |
| AC-2 | Yes | Category filter updates URL and filters products |
| AC-3 | Yes | Sort by price low-to-high reorders grid |
| AC-4 | Yes | Out of Stock badge, disabled Add to Cart button |
| AC-5 | Yes | Infinite scroll loads next 20 products |

---

## Implementation Steps
1. Write failing tests for ProductCard (renders name, price, thumbnail, stock badge, disabled button when out of stock, axe-core)
2. Write failing tests for ProductGrid (renders list of cards, keyboard navigable)
3. Write failing tests for CategoryFilter (renders categories, triggers filter callback, URL update)
4. Write failing tests for useProducts hook (fetches, paginates, filters, sorts)
5. Write failing tests for CataloguePage (integration of all components)
6. Create TypeScript interfaces in `types/product.ts`
7. Create API client in `api/products.ts`
8. Implement ProductCard component with aria-label
9. Implement ProductGrid with responsive CSS grid
10. Implement CategoryFilter dropdown
11. Implement SortControl
12. Implement useProducts hook with IntersectionObserver for infinite scroll
13. Implement useCategories hook
14. Implement CataloguePage — compose all components, manage URL search params
15. Add route in App.tsx
16. Run lint → types → tests → Playwright → verify all green

---

## Dependencies
- Depends on: STORY-001-T002 (API must be available)
- Blocks: (none)

---

## Role Annotations
(empty)
