# STORY-002-T001 — Product Detail API Endpoint

**Status:** PENDING
**Story:** STORY-002 — Product detail page
**Task:** T001 — Implement GET /api/v1/products/:id endpoint
**Assigned to:** Agent
**Priority:** P0
**Sprint:** 1
**Complexity:** S

---

## Summary

Add a single-product detail endpoint that returns full product information including all images. Validate product ID as UUID. Return 404 for non-existent products.

---

## Files in Scope

### CREATE
- `services/api_python/src/types/product_detail.py` — Pydantic schema for full product detail response
- `services/api_python/tests/unit/test_product_detail_route.py`
- `services/api_python/tests/unit/test_product_repository_get_by_id.py`

### MODIFY
- `services/api_python/src/repositories/product_repository.py` — add get_by_id method
- `services/api_python/src/services/product_service.py` — add get_product_detail method
- `services/api_python/src/routes/products.py` — add GET /products/{product_id} route

### OUT OF SCOPE
- Frontend — handled in T002
- Add to cart logic — STORY-005

---

## Acceptance Criteria Covered
| AC   | Covered by Task | Notes |
|------|----------------|-------|
| AC-1 | Partial | API returns all detail fields (name, description, price, images, stock, category) |
| AC-4 | Yes | 404 response for non-existent product ID |

---

## Implementation Steps
1. Write failing test: GET /products/{uuid} returns full product detail
2. Write failing test: GET /products/{non-existent-uuid} returns 404
3. Write failing test: GET /products/{invalid-string} returns 422 (UUID validation)
4. Add ProductDetailResponse Pydantic schema
5. Add get_by_id to product_repository
6. Add get_product_detail to product_service
7. Add route handler with UUID path param validation
8. Run lint → types → tests → verify all green

---

## Dependencies
- Depends on: STORY-001-T001 (Product model), STORY-001-T002 (base routes/repo exist)
- Blocks: STORY-002-T002 (frontend)

---

## Role Annotations
(empty)
