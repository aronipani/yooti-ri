# STORY-004-T002 — Login, Logout, and Token Management API

**Status:** PENDING
**Story:** STORY-004 — Sign in and out
**Task:** T002 — POST /login, POST /logout, POST /refresh with JWT, httpOnly cookies, account lockout, auth middleware
**Layer:** api
**Priority:** P0
**Sprint:** 1
**Complexity:** M (task 2 of 3)

---

## Files in Scope

### CREATE
- `services/api_python/src/services/token_service.py` — JWT encode/decode, refresh token in httpOnly cookie
- `services/api_python/src/repositories/session_repository.py` — create/revoke/validate sessions
- `services/api_python/src/middleware/auth.py` — require_auth dependency (extract JWT, inject current user)
- `services/api_python/src/types/login.py` — LoginRequest, LoginResponse, TokenPayload schemas
- `services/api_python/tests/unit/test_login_route.py`
- `services/api_python/tests/unit/test_auth_middleware.py`

### MODIFY
- `services/api_python/src/services/auth_service.py` — add login, logout, lockout logic
- `services/api_python/src/routes/auth.py` — add POST /login, POST /logout, POST /refresh

### OUT OF SCOPE
- Frontend (T003)
- Rate limiting on auth endpoints (STORY-010)

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-1 | Full | Valid credentials → JWT + httpOnly refresh cookie, stays signed in |
| AC-2 | Full | Invalid credentials → generic "Incorrect email or password" |
| AC-3 | Full | Logout revokes session server-side |
| AC-4 | Full | Expired JWT → 401 |
| AC-5 | Full | 10 failed logins → 30 min lockout with message |

---

## Implementation Steps
1. Write failing test: login with valid creds returns JWT + sets httpOnly cookie
2. Write failing test: login with invalid creds returns generic error
3. Write failing test: logout revokes session (token no longer valid)
4. Write failing test: expired JWT returns 401
5. Write failing test: 10 failures lock account for 30 minutes
6. Write failing test: auth middleware extracts user from valid JWT
7. Implement token_service, session_repository
8. Add login/logout/lockout to auth_service
9. Add routes, implement auth middleware
10. Run lint → types → tests → green

---

## Dependencies
- Depends on: STORY-004-T001 (Session model), STORY-003-T002 (auth_service, auth routes)
- Blocks: STORY-004-T003, STORY-010-T001
