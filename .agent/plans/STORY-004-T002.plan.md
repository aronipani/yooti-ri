# STORY-004-T002 — Login/Logout Frontend and Protected Routes

**Status:** PENDING
**Story:** STORY-004 — Sign in and out
**Task:** T002 — Build login form, sign out button, session persistence, and protected route wrapper
**Assigned to:** Agent
**Priority:** P0
**Sprint:** 1
**Complexity:** S

---

## Summary

Build the login page, sign out functionality in the header, session persistence on refresh (check token on app load), protected route wrapper that redirects to login with expiry message, and update AuthContext with login/logout.

---

## Files in Scope

### CREATE
- `frontend/dashboard/src/pages/LoginPage.tsx` — login form page
- `frontend/dashboard/src/components/LoginForm.tsx` — email + password form
- `frontend/dashboard/src/components/ProtectedRoute.tsx` — wrapper redirecting unauthenticated users
- `frontend/dashboard/src/components/Header.tsx` — header with user name and sign out button
- `frontend/dashboard/tests/unit/LoginForm.test.tsx`
- `frontend/dashboard/tests/unit/LoginPage.test.tsx`
- `frontend/dashboard/tests/unit/ProtectedRoute.test.tsx`
- `frontend/dashboard/tests/unit/Header.test.tsx`

### MODIFY
- `frontend/dashboard/src/hooks/useAuth.ts` — add login, logout, checkSession methods
- `frontend/dashboard/src/api/auth.ts` — add login, logout, refresh API calls
- `frontend/dashboard/src/contexts/AuthContext.tsx` — add login/logout state management
- `frontend/dashboard/src/App.tsx` — add /login route, wrap protected routes with ProtectedRoute

### OUT OF SCOPE
- Registration form — STORY-003-T002
- Rate limit UI — handled by generic error display

---

## Acceptance Criteria Covered
| AC   | Covered by Task | Notes |
|------|----------------|-------|
| AC-1 | Yes | Login redirects to homepage, name in header, persists on refresh |
| AC-2 | Yes | "Incorrect email or password" generic error |
| AC-3 | Yes | Sign out clears session, redirects, protected routes blocked |
| AC-4 | Yes | Expired session → redirect to login with message |

---

## Implementation Steps
1. Write failing tests for LoginForm (renders, validates, shows generic error, axe-core)
2. Write failing tests for ProtectedRoute (redirects when unauthenticated, shows expiry message)
3. Write failing tests for Header (shows user name, sign out button triggers logout)
4. Add login/logout to auth API client
5. Update AuthContext with login, logout, session check on mount
6. Implement LoginForm with aria-describedby errors
7. Implement LoginPage
8. Implement ProtectedRoute
9. Implement Header
10. Update App.tsx with routes and ProtectedRoute wrappers
11. Run lint → types → tests → Playwright → verify all green

---

## Dependencies
- Depends on: STORY-004-T001 (API), STORY-003-T002 (AuthContext exists)
- Blocks: STORY-007 (order history needs protected routes)

---

## Role Annotations
(empty)
