# STORY-003-T002 — Registration Frontend Form

**Status:** PENDING
**Story:** STORY-003 — User registration
**Task:** T002 — Build the registration form with client-side validation and error handling
**Assigned to:** Agent
**Priority:** P0
**Sprint:** 1
**Complexity:** S

---

## Summary

Build the React registration form with email/password/name fields, client-side validation (email format, password length >= 8), server error display (duplicate email, rate limit), auto-sign-in on success, and redirect to homepage.

---

## Files in Scope

### CREATE
- `frontend/dashboard/src/pages/RegisterPage.tsx` — registration page
- `frontend/dashboard/src/components/RegisterForm.tsx` — form with validation
- `frontend/dashboard/src/hooks/useAuth.ts` — auth context/state (register, current user)
- `frontend/dashboard/src/api/auth.ts` — API client for auth endpoints
- `frontend/dashboard/src/contexts/AuthContext.tsx` — auth provider
- `frontend/dashboard/src/types/auth.ts` — TypeScript interfaces
- `frontend/dashboard/tests/unit/RegisterForm.test.tsx`
- `frontend/dashboard/tests/unit/RegisterPage.test.tsx`

### MODIFY
- `frontend/dashboard/src/App.tsx` — add /register route, wrap with AuthProvider

### OUT OF SCOPE
- Login form — STORY-004
- Protected routes — STORY-004

---

## Acceptance Criteria Covered
| AC   | Covered by Task | Notes |
|------|----------------|-------|
| AC-1 | Yes | Form submits, auto-sign-in, redirect to homepage |
| AC-2 | Yes | Duplicate email error shown, form not cleared |
| AC-3 | Yes | Client-side: password < 8 chars error before API call |
| AC-4 | Yes | Client-side: invalid email format error before API call |

---

## Implementation Steps
1. Write failing tests for RegisterForm (renders fields with labels, validates email format, validates password length, shows server errors, form not cleared on error, axe-core)
2. Write failing tests for RegisterPage (submits form, redirects on success)
3. Create auth TypeScript interfaces
4. Create auth API client (register function)
5. Create AuthContext and useAuth hook
6. Implement RegisterForm with aria-describedby error messages
7. Implement RegisterPage
8. Add route and AuthProvider in App.tsx
9. Run lint → types → tests → Playwright → verify all green

---

## Dependencies
- Depends on: STORY-003-T001 (API must exist)
- Blocks: STORY-004-T002 (shared AuthContext)

---

## Role Annotations
(empty)
