# STORY-003-T002 — Registration API with Rate Limiting and Welcome Email

**Status:** PENDING
**Story:** STORY-003 — User registration
**Task:** T002 — POST /api/v1/auth/register with bcrypt hashing, rate limiting, email dispatch
**Layer:** api
**Priority:** P0
**Sprint:** 1
**Complexity:** M (task 2 of 3)

---

## Files in Scope

### CREATE
- `services/api_python/src/repositories/user_repository.py` — create_user, get_by_email
- `services/api_python/src/services/auth_service.py` — register (hash bcrypt cost 12, check dup, create)
- `services/api_python/src/services/email_service.py` — send_welcome_email (async interface)
- `services/api_python/src/routes/auth.py` — POST /api/v1/auth/register
- `services/api_python/src/types/auth.py` — RegisterRequest, RegisterResponse schemas
- `services/api_python/src/middleware/rate_limit.py` — Redis IP rate limiter (configurable per-route)
- `services/api_python/tests/unit/test_auth_service.py`

### MODIFY
- `services/api_python/main.py` — register auth router

### OUT OF SCOPE
- Login/logout/JWT (STORY-004)
- Frontend form (T003)

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-1 | Full | Creates account, returns success response |
| AC-2 | Full | Duplicate email → error, constant-time to prevent enumeration |
| AC-5 | Full | Welcome email triggered on success |
| AC-6 | Full | 5 attempts/15min per IP → 429 |

---

## Implementation Steps
1. Write failing test: register creates user with bcrypt hash (cost 12)
2. Write failing test: duplicate email returns error in constant time
3. Write failing test: rate limiter returns 429 after 5 requests with Retry-After header
4. Write failing test: welcome email service called on success
5. Implement Pydantic schemas
6. Implement user_repository, auth_service, email_service interface
7. Implement rate_limit middleware (Redis INCR/EXPIRE, graceful degradation)
8. Implement register route, register router in main.py
9. Run lint → types → tests → green

---

## Dependencies
- Depends on: STORY-003-T001 (User model, AppError)
- Blocks: STORY-003-T003, STORY-004-T001
[ARCHITECT G2 2026-03-29]: email_service.py is an interface only.
