# STORY-003-T003 — Registration Frontend Form

**Status:** PENDING
**Story:** STORY-003 — User registration
**Task:** T003 — Registration page with client-side validation, error handling, AuthContext
**Layer:** frontend
**Priority:** P0
**Sprint:** 1
**Complexity:** M (task 3 of 3)

---

## Files in Scope

### CREATE
- `frontend/dashboard/src/pages/RegisterPage.tsx` — registration page
- `frontend/dashboard/src/components/RegisterForm.tsx` — form with validation
- `frontend/dashboard/src/contexts/AuthContext.tsx` — auth provider (register, currentUser)
- `frontend/dashboard/src/hooks/useAuth.ts` — auth hook
- `frontend/dashboard/src/api/auth.ts` — register API call
- `frontend/dashboard/src/types/auth.ts` — TypeScript interfaces
- `frontend/dashboard/tests/unit/RegisterPage.test.tsx`

### MODIFY
- `frontend/dashboard/src/App.tsx` — add /register route, wrap with AuthProvider

### OUT OF SCOPE
- Login form (STORY-004)
- Protected routes (STORY-004)

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-1 | Full | Submit → create account → auto sign-in → redirect home |
| AC-2 | Full | Duplicate email error shown, form not cleared |
| AC-3 | Full | Password < 8 chars → client-side error before API call |
| AC-4 | Full | Invalid email → client-side error before API call |

---

## Implementation Steps
1. Write failing tests: RegisterForm validates email format, password length, shows server errors, form preserved on error, axe-core
2. Create TypeScript auth interfaces and API client
3. Create AuthContext and useAuth hook
4. Implement RegisterForm with aria-describedby error messages
5. Implement RegisterPage
6. Add route and AuthProvider in App.tsx
7. Run lint → types → tests → Playwright → green

---

## Dependencies
- Depends on: STORY-003-T002 (API)
- Blocks: STORY-004-T002 (shared AuthContext)
