# STORY-004-T001 — Session Model and Database Schema

**Status:** COMPLETE
**Story:** STORY-004 — Sign in and out
**Task:** T001 — Session model, migration, failed login counter on User
**Layer:** database
**Priority:** P0
**Sprint:** 1
**Complexity:** M (task 1 of 3)

---

## Files in Scope

### CREATE
- `services/api_python/src/models/session.py` — Session (user_id, token_hash, expires_at, is_revoked)
- `services/api_python/alembic/versions/003_create_sessions.py` — migration (sessions table + failed_login fields on users)
- `services/api_python/tests/unit/test_models_session.py`

### MODIFY
- `services/api_python/src/models/user.py` — add failed_login_count, locked_until fields
- `services/api_python/src/models/__init__.py` — export Session

### OUT OF SCOPE
- API routes, token logic
- Frontend

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-3 | Foundation | Session model enables server-side invalidation |
| AC-4 | Foundation | expires_at field for 24h expiry |
| AC-5 | Foundation | failed_login_count + locked_until on User |

---

## Implementation Steps
1. Write failing tests for Session model (fields, FK to User)
2. Write failing tests for User lockout fields
3. Create Session model
4. Add failed_login_count (int default 0) and locked_until (datetime nullable) to User
5. Update `models/__init__.py`
6. Create migration
7. Run lint → types → tests → green

---

## Dependencies
- Depends on: STORY-003-T001 (User model)
- Blocks: STORY-004-T002
