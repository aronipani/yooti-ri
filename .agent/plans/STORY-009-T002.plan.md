# STORY-009-T002 — Admin Image Upload Endpoint

**Status:** PENDING
**Story:** STORY-009 — Admin product management
**Task:** T002 — POST /api/v1/admin/products/:id/images with type and size validation
**Layer:** api
**Priority:** P1
**Sprint:** 2
**Complexity:** L (task 2 of 3)

---

## Files in Scope

### CREATE
- `services/api_python/src/services/image_service.py` — validate type (image/*), validate size (<=5MB), store
- `services/api_python/tests/unit/test_image_upload.py`

### MODIFY
- `services/api_python/src/routes/admin_products.py` — add POST /admin/products/:id/images
- `services/api_python/src/types/admin_product.py` — add ImageUploadResponse schema

### OUT OF SCOPE
- S3/cloud storage (use local/interface)
- Frontend upload UI (T003)

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-5 | Full | >5MB rejected, non-image rejected, both before server processing |

---

## Implementation Steps
1. Write failing test: image upload with valid image succeeds
2. Write failing test: image > 5MB → 413 with error message
3. Write failing test: non-image file type → 415 with error message
4. Implement image_service (validate, store interface)
5. Add upload route to admin_products
6. Add ImageUploadResponse schema
7. Run lint → types → tests → green

---

## Dependencies
- Depends on: STORY-009-T001 (admin routes, require_admin)
- Blocks: STORY-009-T003
