# STORY-008-T002 — Product Search Frontend

**Status:** PENDING
**Story:** STORY-008 — Product search
**Task:** T002 — Search bar with 300ms debounce, results display, clear behaviour
**Layer:** frontend
**Priority:** P1
**Sprint:** 2
**Complexity:** M (task 2 of 2)

---

## Files in Scope

### CREATE
- `frontend/dashboard/src/components/SearchBar.tsx` — input with role=search, 300ms debounce, min 2 chars
- `frontend/dashboard/src/hooks/useSearch.ts` — search state, debounce logic
- `frontend/dashboard/src/api/search.ts` — search API client
- `frontend/dashboard/tests/unit/SearchBar.test.tsx`
- `frontend/dashboard/tests/unit/useSearch.test.tsx`

### MODIFY
- `frontend/dashboard/src/pages/CataloguePage.tsx` — integrate SearchBar, toggle search results vs catalogue

### OUT OF SCOPE
- Backend search (T001)
- Advanced filters

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-1 | Full | 2+ chars, 300ms debounce, matching results shown |
| AC-2 | Full | No results → message with category browse suggestion |
| AC-3 | Full | Max 1 API call per 300ms |
| AC-5 | Full | Clear input restores full catalogue |

---

## Implementation Steps
1. Write failing tests: SearchBar role=search, debounce at 300ms, no call < 2 chars, axe-core
2. Write failing tests: useSearch debounce timing, min chars, clear resets
3. Create search API client
4. Implement useSearch hook with debounce
5. Implement SearchBar with aria-live=polite results region
6. Integrate into CataloguePage
7. Run lint → types → tests → Playwright → green

---

## Dependencies
- Depends on: STORY-008-T001 (API), STORY-001-T003 (CataloguePage)
- Blocks: none
