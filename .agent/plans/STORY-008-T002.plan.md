# STORY-008-T002 — Product Search Frontend

**Status:** PENDING
**Story:** STORY-008 — Product search
**Task:** T002 — Build search bar with debounce, results display, and clear behaviour
**Assigned to:** Agent
**Priority:** P1
**Sprint:** 2
**Complexity:** S

---

## Summary

Build the search UI: search input with role=search, 300ms debounce, results region with aria-live=polite, no-results message with category browse suggestion, clear input restores full catalogue. Minimum 2 characters before searching.

---

## Files in Scope

### CREATE
- `frontend/dashboard/src/components/SearchBar.tsx` — search input with debounce
- `frontend/dashboard/src/components/SearchResults.tsx` — results display region
- `frontend/dashboard/src/hooks/useSearch.ts` — search state with debounce logic
- `frontend/dashboard/src/api/search.ts` — API client for search endpoint
- `frontend/dashboard/tests/unit/SearchBar.test.tsx`
- `frontend/dashboard/tests/unit/SearchResults.test.tsx`
- `frontend/dashboard/tests/unit/useSearch.test.tsx`

### MODIFY
- `frontend/dashboard/src/pages/CataloguePage.tsx` — integrate SearchBar, toggle between search results and catalogue

### OUT OF SCOPE
- Backend search logic — T001
- Advanced filters — future story

---

## Acceptance Criteria Covered
| AC   | Covered by Task | Notes |
|------|----------------|-------|
| AC-1 | Yes | 2+ chars, 300ms debounce, results shown |
| AC-2 | Yes | No results message with category suggestion |
| AC-3 | Yes | Debounce: max 1 API call per 300ms |
| AC-5 | Yes | Clear input restores full catalogue |

---

## Implementation Steps
1. Write failing tests for SearchBar (role=search, debounce fires after 300ms, no call < 2 chars, axe-core)
2. Write failing tests for SearchResults (renders products, no-results message, aria-live=polite)
3. Write failing tests for useSearch (debounce timing, min 2 chars, clear resets)
4. Create search API client
5. Implement useSearch hook with debounce (setTimeout/clearTimeout)
6. Implement SearchBar
7. Implement SearchResults
8. Integrate into CataloguePage
9. Run lint → types → tests → Playwright → verify all green

---

## Dependencies
- Depends on: STORY-008-T001 (API), STORY-001-T003 (CataloguePage exists)
- Blocks: (none)

---

## Role Annotations
(empty)
