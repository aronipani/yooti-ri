# STORY-003-T001 — User Model and Database Schema

**Status:** PENDING
**Story:** STORY-003 — User registration
**Task:** T001 — User model, migration, AppError base class
**Layer:** database
**Priority:** P0
**Sprint:** 1
**Complexity:** M (task 1 of 3)

---

## Files in Scope

### CREATE
- `services/api_python/src/models/user.py` — User model (email unique, password_hash, name, role enum, is_active)
- `services/api_python/src/errors.py` — AppError base class, DuplicateEmailError, ValidationError, RateLimitError
- `services/api_python/alembic/versions/002_create_users.py` — migration
- `services/api_python/tests/unit/test_models_user.py`

### MODIFY
- `services/api_python/src/models/__init__.py` — export User

### OUT OF SCOPE
- API routes, services
- Frontend
- JWT/session (STORY-004)

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-1 | Foundation | User model stores account data |
| AC-2 | Foundation | Email unique constraint enables duplicate detection |

---

## Implementation Steps
1. Write failing tests for User model (fields, email uniqueness, role enum)
2. Write failing tests for AppError hierarchy
3. Create AppError base class and subclasses
4. Create User model with unique email, role enum (user/admin)
5. Update `models/__init__.py`
6. Create Alembic migration
7. Run lint → types → tests → green

---

## Dependencies
- Depends on: none
- Blocks: STORY-003-T002
