# STORY-003-T001 — User Model, Auth Service, and Registration API

**Status:** PENDING
**Story:** STORY-003 — User registration
**Task:** T001 — Create User model, auth service with bcrypt hashing, registration endpoint, welcome email trigger, and rate limiting
**Assigned to:** Agent
**Priority:** P0
**Sprint:** 1
**Complexity:** M

---

## Summary

Build the full registration backend: User model, password hashing with bcrypt (cost 12), POST /api/v1/auth/register endpoint, duplicate email handling (constant-time to prevent enumeration), welcome email dispatch, and IP-based rate limiting (5 attempts/15 min).

---

## Files in Scope

### CREATE
- `services/api_python/src/models/user.py` — User model (id, email unique, password_hash, name, role enum, is_active)
- `services/api_python/src/repositories/user_repository.py` — create_user, get_by_email
- `services/api_python/src/services/auth_service.py` — register (hash password, check duplicate, create user)
- `services/api_python/src/services/email_service.py` — send_welcome_email (async, interface only — actual provider in env config)
- `services/api_python/src/routes/auth.py` — POST /api/v1/auth/register
- `services/api_python/src/types/auth.py` — RegisterRequest, RegisterResponse schemas
- `services/api_python/src/middleware/rate_limit.py` — Redis-backed IP rate limiter
- `services/api_python/src/errors.py` — AppError base class, DuplicateEmailError, RateLimitError
- `services/api_python/alembic/versions/002_create_users.py` — migration
- `services/api_python/tests/unit/test_auth_service.py`
- `services/api_python/tests/unit/test_user_repository.py`
- `services/api_python/tests/unit/test_register_route.py`
- `services/api_python/tests/unit/test_rate_limit_middleware.py`

### MODIFY
- `services/api_python/src/models/__init__.py` — export User
- `services/api_python/main.py` — register auth router

### OUT OF SCOPE
- Login/logout — STORY-004
- JWT token generation — STORY-004
- Frontend registration form — T002

---

## Acceptance Criteria Covered
| AC   | Covered by Task | Notes |
|------|----------------|-------|
| AC-1 | Partial | API creates account, returns success + token |
| AC-2 | Yes | Duplicate email returns error (constant-time) |
| AC-5 | Yes | Welcome email triggered on registration |
| AC-6 | Yes | Rate limiting: 5 attempts/15min per IP → 429 |

---

## Implementation Steps
1. Write failing test: auth_service.register creates user with bcrypt-hashed password (cost 12)
2. Write failing test: auth_service.register with duplicate email returns error in constant time
3. Write failing test: POST /register with valid data returns 201
4. Write failing test: POST /register with existing email returns 409
5. Write failing test: rate limiter returns 429 after 5 requests
6. Write failing test: welcome email service called on successful registration
7. Create AppError base class and specific error subclasses
8. Create User model with email uniqueness constraint
9. Create Alembic migration
10. Implement user_repository (create_user, get_by_email)
11. Implement auth_service.register — hash, check dup, create, trigger email
12. Implement email_service interface (send_welcome_email)
13. Implement rate_limit middleware (Redis INCR with EXPIRE)
14. Implement auth route with rate limiting applied
15. Register auth router in main.py
16. Run lint → types → tests → verify all green

---

## Dependencies
- Depends on: (none — User model is independent)
- Blocks: STORY-003-T002 (frontend), STORY-004 (login needs User model)

---

## Role Annotations
(empty)
