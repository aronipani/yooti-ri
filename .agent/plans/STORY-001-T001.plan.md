# STORY-001-T001 — Product and Category Database Models

**Status:** COMPLETE
**Story:** STORY-001 — Browse product catalogue
**Task:** T001 — Database schema: Product and Category models, migration, seed data
**Layer:** database
**Priority:** P0
**Sprint:** 1
**Complexity:** M (task 1 of 3)

---

## Files in Scope

### CREATE
- `services/api_python/src/models/product.py` — Product model
- `services/api_python/src/models/category.py` — Category model
- `services/api_python/alembic/versions/001_create_products_categories.py` — migration
- `services/api_python/scripts/seed_products.py` — dev seed data (>=25 products, >=3 categories)
- `services/api_python/tests/unit/test_models_product.py`

### MODIFY
- `services/api_python/src/models/__init__.py` — export Product, Category

### OUT OF SCOPE
- API routes, services, repositories
- Frontend components

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-1 | Foundation | Product fields: name, price, thumbnail_url, stock_quantity |
| AC-2 | Foundation | Category model with slug enables filtering |
| AC-4 | Foundation | stock_quantity field enables out-of-stock detection |

---

## Implementation Steps
1. Write failing unit tests for Product model (fields, constraints, FK to Category)
2. Write failing unit tests for Category model (slug unique constraint)
3. Create Category model: id, name, slug (unique), timestamps from Base
4. Create Product model: id, name, description, price (Numeric 10,2), stock_quantity (int default 0), category_id (FK), thumbnail_url, images (JSON), is_active (bool default true), timestamps from Base
5. Update `models/__init__.py` exports
6. Create Alembic migration
7. Create seed script
8. Run lint → types → tests → green

---

## Dependencies
- Depends on: none (foundational)
- Blocks: STORY-001-T002
