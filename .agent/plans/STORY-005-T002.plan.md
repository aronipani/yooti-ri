# STORY-005-T002 — Cart API Endpoints

**Status:** COMPLETE
**Story:** STORY-005 — Shopping cart
**Task:** T002 — Cart CRUD API with stock validation, guest Redis cart, price calculation
**Layer:** api
**Priority:** P0
**Sprint:** 1
**Complexity:** L (task 2 of 3)

---

## Files in Scope

### CREATE
- `services/api_python/src/repositories/cart_repository.py` — CRUD for cart and items
- `services/api_python/src/services/cart_service.py` — add, update, remove, stock validation, guest/auth, subtotal/tax
- `services/api_python/src/routes/cart.py` — GET /cart, POST /cart/items, PUT /cart/items/:productId, DELETE /cart/items/:productId
- `services/api_python/src/types/cart.py` — Pydantic schemas (CartResponse, CartItemResponse, AddToCartRequest)
- `services/api_python/tests/unit/test_cart_service.py`

### MODIFY
- `services/api_python/main.py` — register cart router

### OUT OF SCOPE
- Frontend (T003)
- Checkout / order creation (STORY-006)

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-1 | Full | Add item → returns cart with name, price, qty, subtotal |
| AC-2 | Full | Duplicate product → increment quantity |
| AC-3 | Full | PUT quantity=0 → remove item |
| AC-4 | Full | Guest cart in Redis with 7-day TTL |
| AC-5 | Full | GET /cart validates stock, adjusts qty, returns warning |
| AC-6 | Full | Response includes subtotal, estimated tax |

---

## Implementation Steps
1. Write failing test: add item creates cart item with correct data
2. Write failing test: add duplicate product increments quantity
3. Write failing test: update quantity to 0 removes item
4. Write failing test: GET /cart detects stock drop and adjusts quantity with warning
5. Write failing test: guest cart uses Redis with 7-day TTL
6. Write failing test: response includes subtotal and estimated tax
7. Implement cart_repository, cart_service, Pydantic schemas
8. Implement cart routes, register router
9. Run lint → types → tests → green

---

## Dependencies
- Depends on: STORY-005-T001 (models), STORY-004-T002 (auth middleware for authenticated carts)
- Blocks: STORY-005-T003, STORY-006-T001
