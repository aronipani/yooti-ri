# STORY-005-T002 — Cart Frontend: Drawer, Cart Icon, and Quantity Controls

**Status:** PENDING
**Story:** STORY-005 — Shopping cart management
**Task:** T002 — Build cart drawer, cart icon with count badge, quantity controls, and stock warnings
**Assigned to:** Agent
**Priority:** P0
**Sprint:** 1
**Complexity:** M

---

## Summary

Build the cart UI: a slide-out cart drawer showing items with name, price, quantity, subtotal; quantity input controls; remove button; stock warning display; order subtotal and estimated tax; Proceed to Checkout button. Cart icon in header shows item count. Screen reader announcements on cart updates.

---

## Files in Scope

### CREATE
- `frontend/dashboard/src/components/CartDrawer.tsx` — slide-out cart panel
- `frontend/dashboard/src/components/CartItem.tsx` — individual cart line item
- `frontend/dashboard/src/components/CartIcon.tsx` — header cart icon with count badge
- `frontend/dashboard/src/components/CartSummary.tsx` — subtotal, tax, checkout button
- `frontend/dashboard/src/hooks/useCart.ts` — cart state management (fetch, add, update, remove)
- `frontend/dashboard/src/contexts/CartContext.tsx` — cart provider
- `frontend/dashboard/src/api/cart.ts` — API client for cart endpoints
- `frontend/dashboard/src/types/cart.ts` — TypeScript interfaces
- `frontend/dashboard/tests/unit/CartDrawer.test.tsx`
- `frontend/dashboard/tests/unit/CartItem.test.tsx`
- `frontend/dashboard/tests/unit/CartIcon.test.tsx`
- `frontend/dashboard/tests/unit/CartSummary.test.tsx`
- `frontend/dashboard/tests/unit/useCart.test.tsx`

### MODIFY
- `frontend/dashboard/src/components/Header.tsx` — add CartIcon
- `frontend/dashboard/src/components/ProductCard.tsx` — wire Add to Cart button to cart context
- `frontend/dashboard/src/pages/ProductDetailPage.tsx` — wire Add to Cart to cart context
- `frontend/dashboard/src/App.tsx` — wrap with CartProvider

### OUT OF SCOPE
- Checkout page — STORY-006
- Order creation — STORY-006

---

## Acceptance Criteria Covered
| AC   | Covered by Task | Notes |
|------|----------------|-------|
| AC-1 | Yes | Cart icon updated, item in drawer with name/price/qty/subtotal |
| AC-2 | Yes | Duplicate add increments (handled by API, UI reflects) |
| AC-3 | Yes | Quantity to 0 removes item |
| AC-4 | Yes | Cart persists via API (guest or auth) |
| AC-5 | Yes | Stock warning displayed, qty adjusted |
| AC-6 | Yes | Subtotal, tax, Proceed to Checkout button shown |

---

## Implementation Steps
1. Write failing tests for CartItem (renders name, price, qty, subtotal; qty controls; remove button with aria-label; axe-core)
2. Write failing tests for CartDrawer (lists items, shows empty state, stock warnings)
3. Write failing tests for CartIcon (shows count badge, opens drawer)
4. Write failing tests for CartSummary (subtotal, tax, checkout button)
5. Write failing tests for useCart hook (add, update, remove, fetch)
6. Create TypeScript interfaces
7. Create cart API client
8. Implement CartContext and useCart hook
9. Implement CartItem with accessible quantity inputs
10. Implement CartDrawer with aria-live region
11. Implement CartIcon
12. Implement CartSummary
13. Update Header, ProductCard, ProductDetailPage to use cart
14. Wrap App with CartProvider
15. Run lint → types → tests → Playwright → verify all green

---

## Dependencies
- Depends on: STORY-005-T001 (API), STORY-002-T002 (ProductDetailPage), STORY-001-T003 (ProductCard)
- Blocks: STORY-006 (checkout needs cart UI)

---

## Role Annotations
(empty)
