# STORY-006-T002 — Checkout API with Payment and Stock Atomicity

**Status:** PENDING
**Story:** STORY-006 — Checkout and order placement
**Task:** T002 — POST /api/v1/orders with server-side price recalculation, atomic stock decrement, payment gateway, idempotency
**Layer:** api
**Priority:** P0
**Sprint:** 2
**Complexity:** XL (task 2 of 4)

---

## Files in Scope

### CREATE
- `services/api_python/src/repositories/order_repository.py` — create order with SELECT FOR UPDATE stock decrement
- `services/api_python/src/services/order_service.py` — checkout logic (validate, recalc, atomic decrement, payment, create order)
- `services/api_python/src/services/payment_service.py` — payment gateway interface (charge, refund)
- `services/api_python/src/routes/orders.py` — POST /api/v1/orders, GET /api/v1/orders/:id
- `services/api_python/src/types/order.py` — CreateOrderRequest, OrderResponse schemas
- `services/api_python/tests/unit/test_order_service.py`

### MODIFY
- `services/api_python/main.py` — register orders router

### OUT OF SCOPE
- Confirmation email (T003)
- Frontend (T004)
- Order history listing (STORY-007)

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-2 | Full | Returns order number, item summary, delivery estimate |
| AC-4 | Full | Out-of-stock at checkout → order rejected, item identified |
| AC-5 | Full | Payment failure → no order, no stock reserved |
| AC-6 | Full | Concurrent purchase → SELECT FOR UPDATE, one succeeds |

---

## Implementation Steps
1. Write failing test: create_order recalculates prices server-side
2. Write failing test: atomic stock decrement with SELECT FOR UPDATE
3. Write failing test: concurrent last-unit orders — one succeeds, one fails
4. Write failing test: idempotency key — same key returns same order
5. Write failing test: payment failure rolls back order and stock
6. Write failing test: out-of-stock item → 409 with item identified
7. Write failing test: payment details never in logs
8. Implement payment_service interface, order_repository, order_service
9. Implement routes, register router
10. Run lint → types → tests → green

---

## Dependencies
- Depends on: STORY-006-T001 (models), STORY-005-T002 (cart service), STORY-004-T002 (auth middleware)
- Blocks: STORY-006-T003, STORY-006-T004, STORY-007-T001
