# STORY-002-T002 — Product Detail Frontend Page

**Status:** PENDING
**Story:** STORY-002 — Product detail page
**Task:** T002 — Build the product detail page with image gallery, quantity selector, and Add to Cart
**Assigned to:** Agent
**Priority:** P0
**Sprint:** 1
**Complexity:** S

---

## Summary

Build the React product detail page showing full product info, image gallery with thumbnail selection, quantity selector capped at available stock, Add to Cart button, and a 404 page for missing products.

---

## Files in Scope

### CREATE
- `frontend/dashboard/src/pages/ProductDetailPage.tsx` — page component
- `frontend/dashboard/src/components/ImageGallery.tsx` — main image + thumbnails
- `frontend/dashboard/src/components/QuantitySelector.tsx` — quantity input with stock cap
- `frontend/dashboard/src/components/NotFoundPage.tsx` — 404 page with link to catalogue
- `frontend/dashboard/src/hooks/useProduct.ts` — fetch single product by ID
- `frontend/dashboard/src/api/products.ts` — add getProductById function (modify existing)
- `frontend/dashboard/tests/unit/ProductDetailPage.test.tsx`
- `frontend/dashboard/tests/unit/ImageGallery.test.tsx`
- `frontend/dashboard/tests/unit/QuantitySelector.test.tsx`
- `frontend/dashboard/tests/unit/NotFoundPage.test.tsx`

### MODIFY
- `frontend/dashboard/src/App.tsx` — add route /products/:id
- `frontend/dashboard/src/api/products.ts` — add getProductById

### OUT OF SCOPE
- Cart state management — STORY-005
- Add to Cart API call — STORY-005

---

## Acceptance Criteria Covered
| AC   | Covered by Task | Notes |
|------|----------------|-------|
| AC-1 | Yes | Full product info displayed |
| AC-2 | Yes | Thumbnail click updates main image |
| AC-3 | Yes | Quantity capped at stock, message shown |
| AC-4 | Yes | 404 page with catalogue link |
| AC-5 | Partial | Add to Cart button present; actual cart integration deferred to STORY-005 |

---

## Implementation Steps
1. Write failing tests for ImageGallery (renders thumbnails, click changes main image, alt text, axe-core)
2. Write failing tests for QuantitySelector (min=1, max=stock, shows message when capped, label+min/max attributes)
3. Write failing tests for NotFoundPage (renders message, link to catalogue)
4. Write failing tests for ProductDetailPage (loads product, shows 404 on error, composes gallery+qty+button)
5. Implement TypeScript interfaces for product detail
6. Add getProductById to API client
7. Implement useProduct hook
8. Implement ImageGallery component
9. Implement QuantitySelector component
10. Implement NotFoundPage component
11. Implement ProductDetailPage — compose all sub-components
12. Add route in App.tsx
13. Run lint → types → tests → Playwright → verify all green

---

## Dependencies
- Depends on: STORY-002-T001 (API endpoint), STORY-001-T003 (shared types/API client)
- Blocks: STORY-005 (cart Add to Cart integration)

---

## Role Annotations
(empty)
