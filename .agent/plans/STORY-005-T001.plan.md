# STORY-005-T001 — Cart Database Schema

**Status:** PENDING
**Story:** STORY-005 — Shopping cart
**Task:** T001 — Cart and CartItem models, migration
**Layer:** database
**Priority:** P0
**Sprint:** 1
**Complexity:** L (task 1 of 3)

---

## Files in Scope

### CREATE
- `services/api_python/src/models/cart.py` — Cart (user_id nullable, session_id for guests, status enum)
- `services/api_python/src/models/cart_item.py` — CartItem (cart_id, product_id, quantity)
- `services/api_python/alembic/versions/004_create_carts.py` — migration
- `services/api_python/tests/unit/test_models_cart.py`

### MODIFY
- `services/api_python/src/models/__init__.py` — export Cart, CartItem

### OUT OF SCOPE
- API routes, services
- Frontend
- Redis guest cart logic (T002 — service layer)

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-1 | Foundation | CartItem stores product_id, quantity |
| AC-2 | Foundation | Unique constraint on (cart_id, product_id) enables dedup |
| AC-4 | Foundation | session_id field enables guest cart identification |

---

## Implementation Steps
1. Write failing tests for Cart model (fields, user_id nullable, session_id)
2. Write failing tests for CartItem model (fields, unique constraint on cart+product)
3. Create Cart and CartItem models
4. Update `models/__init__.py`
5. Create migration
6. Run lint → types → tests → green

---

## Dependencies
- Depends on: STORY-001-T001 (Product model), STORY-003-T001 (User model)
- Blocks: STORY-005-T002
