# STORY-005-T001 — Cart API and Server-Side Cart Management

**Status:** PENDING
**Story:** STORY-005 — Shopping cart management
**Task:** T001 — Implement cart API endpoints with server-side storage and stock validation
**Assigned to:** Agent
**Priority:** P0
**Sprint:** 1
**Complexity:** M

---

## Summary

Build cart backend: Cart and CartItem models (DB for authenticated, Redis for guests with 7-day TTL), CRUD endpoints, stock validation at view time, price calculated server-side. Guest carts merge on login.

---

## Files in Scope

### CREATE
- `services/api_python/src/models/cart.py` — Cart model (id, user_id nullable, session_id for guests, status)
- `services/api_python/src/models/cart_item.py` — CartItem model (cart_id, product_id, quantity, added_at)
- `services/api_python/src/repositories/cart_repository.py` — CRUD for cart and items
- `services/api_python/src/services/cart_service.py` — business logic (add, update qty, remove, stock validation, guest/auth handling)
- `services/api_python/src/routes/cart.py` — GET /cart, POST /cart/items, PUT /cart/items/:productId, DELETE /cart/items/:productId
- `services/api_python/src/types/cart.py` — Pydantic schemas (CartResponse, CartItemResponse, AddToCartRequest)
- `services/api_python/alembic/versions/004_create_carts.py` — migration
- `services/api_python/tests/unit/test_cart_service.py`
- `services/api_python/tests/unit/test_cart_repository.py`
- `services/api_python/tests/unit/test_cart_routes.py`

### MODIFY
- `services/api_python/src/models/__init__.py` — export Cart, CartItem
- `services/api_python/main.py` — register cart router

### OUT OF SCOPE
- Frontend — T002
- Checkout — STORY-006
- Payment — STORY-006

---

## Acceptance Criteria Covered
| AC   | Covered by Task | Notes |
|------|----------------|-------|
| AC-1 | Partial | API adds item, returns updated cart with name, price, qty, subtotal |
| AC-2 | Yes | Adding same product increments quantity |
| AC-3 | Yes | PUT quantity=0 removes item |
| AC-4 | Yes | Guest cart persisted in Redis with 7-day TTL |
| AC-5 | Yes | Stock validation at GET /cart — adjusts qty and returns warning |
| AC-6 | Partial | API returns subtotal and tax calculation |

---

## Implementation Steps
1. Write failing test: add item to cart creates cart item
2. Write failing test: add duplicate product increments quantity
3. Write failing test: update quantity to 0 removes item
4. Write failing test: GET /cart validates stock and adjusts quantities
5. Write failing test: guest cart uses Redis with TTL
6. Write failing test: cart returns subtotal and estimated tax
7. Create Cart and CartItem models, migration
8. Implement cart_repository
9. Implement cart_service with stock validation logic
10. Implement Pydantic schemas
11. Implement cart routes
12. Register router in main.py
13. Run lint → types → tests → verify all green

---

## Dependencies
- Depends on: STORY-001-T001 (Product model), STORY-003-T001 (User model, auth middleware from STORY-004)
- Blocks: STORY-005-T002 (frontend), STORY-006 (checkout)

---

## Role Annotations
(empty)
