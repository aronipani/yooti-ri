# STORY-007-T001 — Order History API Endpoints

**Status:** PENDING
**Story:** STORY-007 — Order history
**Task:** T001 — Implement GET /api/v1/orders (list) and GET /api/v1/orders/:id (detail) with auth scoping
**Assigned to:** Agent
**Priority:** P1
**Sprint:** 2
**Complexity:** S

---

## Summary

Build order history API: paginated order list scoped to authenticated user (user_id from JWT, not request body), order detail endpoint, IDOR prevention (404 not 403 for other users' orders), pagination (10 per page).

---

## Files in Scope

### CREATE
- `services/api_python/src/types/order_list.py` — Pydantic schemas for order list response with pagination
- `services/api_python/tests/unit/test_order_list_route.py`
- `services/api_python/tests/unit/test_order_idor.py`

### MODIFY
- `services/api_python/src/repositories/order_repository.py` — add list_by_user with pagination
- `services/api_python/src/services/order_service.py` — add list_user_orders, get_user_order (scoped)
- `services/api_python/src/routes/orders.py` — add GET /orders with pagination, update GET /orders/:id to scope by user

### OUT OF SCOPE
- Frontend — T002
- Admin order views — future story

---

## Acceptance Criteria Covered
| AC   | Covered by Task | Notes |
|------|----------------|-------|
| AC-1 | Yes | List orders sorted by date descending with order number, date, status, total |
| AC-2 | Yes | Order detail with items, quantities, prices, address, status |
| AC-3 | Yes | IDOR prevention: other user's order returns 404 |
| AC-4 | Yes | Pagination: 10 per page with page controls |

---

## Implementation Steps
1. Write failing test: GET /orders returns user's orders sorted by date desc
2. Write failing test: GET /orders with pagination returns 10 per page
3. Write failing test: GET /orders/:id for another user's order returns 404
4. Write failing test: unauthenticated request returns 401
5. Add list_by_user to order_repository (paginated, user-scoped)
6. Add list_user_orders and get_user_order to order_service
7. Add pagination schemas
8. Update order routes with auth dependency
9. Run lint → types → tests → verify all green

---

## Dependencies
- Depends on: STORY-006-T001 (Order model and base routes), STORY-004-T001 (auth middleware)
- Blocks: STORY-007-T002 (frontend)

---

## Role Annotations
(empty)
