# STORY-007-T001 — Order History API

**Status:** PENDING
**Story:** STORY-007 — Order history
**Task:** T001 — GET /api/v1/orders (paginated, user-scoped) with IDOR prevention
**Layer:** api
**Priority:** P1
**Sprint:** 2
**Complexity:** M (task 1 of 2)

---

## Files in Scope

### CREATE
- `services/api_python/src/types/order_list.py` — OrderListResponse, OrderSummary schemas
- `services/api_python/tests/unit/test_order_history_route.py`

### MODIFY
- `services/api_python/src/repositories/order_repository.py` — add list_by_user (paginated, sorted desc)
- `services/api_python/src/services/order_service.py` — add list_user_orders, scope get_order to user
- `services/api_python/src/routes/orders.py` — add GET /orders with ?page=&limit=10, update GET /orders/:id to scope by user

### OUT OF SCOPE
- Frontend (T002)
- Admin order views

---

## Acceptance Criteria Covered
| AC   | Coverage | Notes |
|------|----------|-------|
| AC-1 | Full | List orders sorted by date desc: order number, date, status, total |
| AC-2 | Full | Order detail: items, quantities, prices, address, status |
| AC-3 | Full | Other user's order → 404 (not 403) |
| AC-4 | Full | 10 per page with pagination |

---

## Implementation Steps
1. Write failing test: GET /orders returns current user's orders sorted date desc
2. Write failing test: pagination with 10 per page
3. Write failing test: GET /orders/:id for other user → 404
4. Write failing test: unauthenticated → 401
5. Add list_by_user to order_repository, scope logic to order_service
6. Add pagination schemas
7. Update order routes with auth dependency and user scoping
8. Run lint → types → tests → green

---

## Dependencies
- Depends on: STORY-006-T002 (Order model, base routes), STORY-004-T002 (auth middleware)
- Blocks: STORY-007-T002
